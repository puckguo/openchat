# =============================================================================
# Alibaba Cloud RDS PostgreSQL Configuration
# =============================================================================

# RDS Host (find in Alibaba Cloud RDS console)
VITE_RDS_HOST=your-rds-host.pg.rds.aliyuncs.com

# RDS Port (default: 5432 for PostgreSQL)
VITE_RDS_PORT=5432

# Database name
VITE_RDS_DATABASE=your_database_name

# Database user
VITE_RDS_USER=your_db_user

# Database password
VITE_RDS_PASSWORD=your_secure_password

# Enable SSL connection (optional)
VITE_RDS_SSL=false

# =============================================================================
# Alibaba Cloud OSS Configuration
# =============================================================================

# AccessKey ID (create in RAM console)
VITE_OSS_ACCESS_KEY_ID=your_access_key_id

# AccessKey Secret
VITE_OSS_ACCESS_KEY_SECRET=your_access_key_secret

# OSS Bucket name
VITE_OSS_BUCKET=your-bucket-name

# OSS Region (e.g., oss-cn-beijing, oss-cn-shanghai, oss-cn-hangzhou)
VITE_OSS_REGION=oss-cn-beijing

# Optional: RAM Role ARN for STS (if using role-based access)
# VITE_OSS_ROLE_ARN=acs:ram::your-account-id:role/your-role-name

# =============================================================================
# Alternative Environment Variable Names (also supported)
# =============================================================================

# These alternative names are also supported if you prefer not using VITE_ prefix
# RDS_HOST=localhost
# RDS_PORT=5432
# RDS_DATABASE=app_db
# RDS_USER=postgres
# RDS_PASSWORD=password
# OSS_ACCESS_KEY_ID=
# OSS_ACCESS_KEY_SECRET=
# OSS_BUCKET=
# OSS_REGION=
